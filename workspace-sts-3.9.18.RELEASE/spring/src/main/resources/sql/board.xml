<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardns">
	
	<!-- 글 작성 -->
	<insert id="insert" parameterType="board">
		<!-- no컬럼은 myboard_seq를 이용해서 입력
			 readcount컬럼은 초기값0, register는 sysdate함수로 입력 -->
		insert into myboard values(myboard_seq.nextval,#{writer},
				#{passwd},#{subject},#{content},0,sysdate)
	</insert>
	
	
	<!-- 글 갯수 -->
	<select id="count" resultType="int">
		select count(*) from myboard
	</select>
	
	
	<!-- 글 목록  -->
		<!-- subQuery사용 -->
	<select id="list" parameterType="int" resultType="board">
	    select * from (
	        select rownum rnum, board.* from (
	            select * from myboard order by no desc
	        ) board
    	) where rnum >= #{startRow} and rnum <= #{endRow}
<!-- 		select * from ( select rownum rnum, board.* from (
		select * from myboard order by no desc) board )
		where rnum >= ((#{page}-1)*10 + 1) and rnum <= ((#{page}-1)*10) -->
	</select>
	
</mapper>


